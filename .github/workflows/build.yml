name: Build ScreenScale16 RootHide

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          set -e

          sudo apt-get update

          sudo apt-get install -y \
            git \
            make \
            clang \
            llvm \
            perl \
            python3 \
            curl \
            wget \
            unzip \
            xz-utils \
            zstd \
            lzma \
            dpkg \
            ldid

      - name: Install RootHide Theos
        run: |
          set -e

          export THEOS=/home/runner/theos

          rm -rf "$THEOS"
          mkdir -p "$THEOS"

          git clone \
            --depth=1 \
            --recursive \
            https://github.com/roothide/theos.git \
            "$THEOS"

          echo "THEOS=$THEOS" >> "$GITHUB_ENV"

          echo "===== THEOS ====="
          ls -lah "$THEOS"

      - name: Install iOS 16.5 SDK
        run: |
          set -e

          export THEOS=/home/runner/theos

          mkdir -p "$THEOS/sdks"

          SDK_URL="https://github.com/theos/sdks/releases/download/master-146e41f/iPhoneOS16.5.sdk.tar.xz"

          echo "Downloading:"
          echo "$SDK_URL"

          curl \
            --fail \
            --location \
            --retry 5 \
            --retry-delay 3 \
            "$SDK_URL" \
            -o /tmp/iPhoneOS16.5.sdk.tar.xz

          echo "===== SDK ARCHIVE ====="
          ls -lh /tmp/iPhoneOS16.5.sdk.tar.xz

          tar \
            -xJf \
            /tmp/iPhoneOS16.5.sdk.tar.xz \
            -C "$THEOS/sdks"

          echo "===== INSTALLED SDK ====="
          ls -lah "$THEOS/sdks"

          test -d "$THEOS/sdks/iPhoneOS16.5.sdk"

      - name: Install iOS toolchain
        run: |
          set -e

          export THEOS=/home/runner/theos

          mkdir -p "$THEOS/toolchain"

          ARCH="$(uname -m)"

          case "$ARCH" in
            x86_64)
              TOOLCHAIN_ARCH="linux-x86_64"
              ;;
            aarch64|arm64)
              TOOLCHAIN_ARCH="linux-arm64"
              ;;
            *)
              echo "Unsupported runner architecture: $ARCH"
              exit 1
              ;;
          esac

          TOOLCHAIN_URL="https://github.com/L1ghtmann/llvm-project/releases/latest/download/iOSToolchain-${TOOLCHAIN_ARCH}.tar.xz"

          echo "Architecture: $ARCH"
          echo "Toolchain:"
          echo "$TOOLCHAIN_URL"

          curl \
            --fail \
            --location \
            --retry 5 \
            --retry-delay 3 \
            "$TOOLCHAIN_URL" \
            -o /tmp/iOSToolchain.tar.xz

          echo "===== TOOLCHAIN ARCHIVE ====="
          ls -lh /tmp/iOSToolchain.tar.xz

          tar \
            -xJf \
            /tmp/iOSToolchain.tar.xz \
            -C "$THEOS/toolchain"

          echo "===== TOOLCHAIN ====="
          find "$THEOS/toolchain" \
            -maxdepth 3 \
            -type f \
            \( \
              -name clang \
              -o -name clang++ \
              -o -name ld64.lld \
              -o -name ldid \
            \) \
            -print

      - name: Verify build environment
        run: |
          set -e

          export THEOS=/home/runner/theos

          echo "================================"
          echo "THEOS"
          echo "================================"
          echo "$THEOS"

          echo "================================"
          echo "SDK"
          echo "================================"
          test -d "$THEOS/sdks/iPhoneOS16.5.sdk"
          ls -lah "$THEOS/sdks/iPhoneOS16.5.sdk"

          echo "================================"
          echo "SDK Settings"
          echo "================================"
          test -f "$THEOS/sdks/iPhoneOS16.5.sdk/SDKSettings.json"

          echo "================================"
          echo "TOOLCHAIN"
          echo "================================"
          find "$THEOS/toolchain" \
            -type f \
            \( -name clang -o -name clang++ \) \
            -print

          echo "================================"
          echo "PACKAGE SCHEMES"
          echo "================================"

          if [ -d "$THEOS/package" ]; then
            find "$THEOS/package" \
              -maxdepth 3 \
              -type f \
              | sort
          fi

          echo "================================"
          echo "ROOTHIDE SCHEME CHECK"
          echo "================================"

          if grep -R "roothide" "$THEOS/makefiles" "$THEOS/package" 2>/dev/null | head -20; then
            echo "RootHide package scheme detected."
          else
            echo "WARNING: RootHide package scheme was not found."
          fi

      - name: Build RootHide package
        run: |
          set -e

          export THEOS=/home/runner/theos

          echo "================================"
          echo "MAKE CLEAN"
          echo "================================"

          make clean

          echo "================================"
          echo "BUILD"
          echo "================================"

          make package \
            FINALPACKAGE=1 \
            THEOS_PACKAGE_SCHEME=roothide

      - name: Inspect package
        run: |
          set -e

          echo "================================"
          echo "PACKAGES"
          echo "================================"

          ls -lah packages/

          echo "================================"
          echo "DEB INFO"
          echo "================================"

          for deb in packages/*.deb; do
            echo "===== $deb ====="
            dpkg-deb -I "$deb"
          done

          echo "================================"
          echo "DEB CONTENT"
          echo "================================"

          for deb in packages/*.deb; do
            dpkg-deb -c "$deb"
          done

      - name: Upload DEB
        uses: actions/upload-artifact@v4
        with:
          name: ScreenScale16-RootHide
          path: packages/*.deb
          if-no-files-found: error
